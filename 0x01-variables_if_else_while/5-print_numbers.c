#include <stdio.h>

/**
 * main - Printing all single digits of base 10
 * Return: Always 0 (Success)
 */

int main(void)
{
	int m = 0;

	while (m <= 9)
	{
	printf("%d", m);
	++m;
	}
	printf("\n");

	return (0);
}
